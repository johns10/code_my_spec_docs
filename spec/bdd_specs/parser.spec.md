# BddSpecs.Parser

Parses Spex DSL files into Spec structs using Elixir AST analysis. Extracts spex, scenario, given , when , and then_ macro calls from source files. Returns structured data without executing the specs.
