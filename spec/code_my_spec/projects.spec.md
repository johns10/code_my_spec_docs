# CodeMySpec.Projects

Per-project records (id, name, local_path, docs_repo, account_id). Project creation from /app's wizard sets the new project active; the local app resolves scope from `local_path` against the current working directory.

## Type

context
