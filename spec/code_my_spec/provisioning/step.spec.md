# CodeMySpec.Provisioning.Step

One named step in the setup sequence and its state — not started, running, done, errored, or paused awaiting an action Sam must take elsewhere. Records what the step attempted and what the provider returned so a failure can be handed to the setup agent with context.

## Type

schema
