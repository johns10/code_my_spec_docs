# CodeMySpec.Provisioning.Widget

Installs the feedback widget on the deployed site by running the existing widget generators and wiring the project credential. Open to anonymous visitors, since the most useful feedback comes from people who never signed up. Degrades quietly rather than breaking the page when the backend is unreachable. Not called done until a real message has travelled from the deployed site into CodeMySpec.

## Type

module
