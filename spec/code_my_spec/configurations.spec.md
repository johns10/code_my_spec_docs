# CodeMySpec.Configurations

Per-project local quality-gate settings (`require_specs`, `require_reviews`, `require_unit_tests`). Filters the requirement graph without breaking edges or producing vacuous actionable nodes.

## Type

context

## Dependencies

- CodeMySpec.Quality
