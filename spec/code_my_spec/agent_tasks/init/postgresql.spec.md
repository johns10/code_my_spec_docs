# CodeMySpec.AgentTasks.Init.Postgresql

## Type

module

Init step: verifies PostgreSQL client is on PATH by shelling out to `psql --version`. `command/2` produces Homebrew install instructions (macOS).
