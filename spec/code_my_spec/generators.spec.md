# CodeMySpec.Generators

Deploy boilerplate a generated project ships with: Dockerfile, CI workflow, per-environment deploy configuration, SOPS env scaffolding, and a health route outside all router pipelines. Always emitted, never behind a flag, so the setup routine fills in values instead of generating files. The templates themselves live in the cms_new generator repo; this component represents them in the architecture.

## Type

context
