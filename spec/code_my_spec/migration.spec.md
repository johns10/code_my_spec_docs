# CodeMySpec.Migration

Copies a project's authored records from a local harness to the cloud. Stories, criteria, rules, personas, epics, issues, questions and project configuration — never files, problems, sessions or components, which the cloud rebuilds from the working copy.

## Type

context
