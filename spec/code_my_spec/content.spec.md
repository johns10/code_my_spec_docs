# CodeMySpec.Content

Generator-output rendering surface dropped into deployed client apps. Receives content pushes from ContentAdmin, persists locally, renders `/blog/:slug`, `/pages/:slug`, `/landing/:slug`, `/documentation/:slug` honoring publish_at/expires_at and tag filters.

## Type

context
