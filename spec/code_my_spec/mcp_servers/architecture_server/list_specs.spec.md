# McpServers.Architecture.Tools.ListSpecs

Lists all component specs in the project scope. Returns component metadata from database (module_name, type, description, name) with file paths. Supports optional filtering by component type (context, schema, repository, module, etc.).
