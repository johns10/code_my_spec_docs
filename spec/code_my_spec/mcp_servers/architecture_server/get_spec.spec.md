# MCPServers.Architecture.Tools.GetSpec

Retrieves a specific component spec by module name or ID. Returns component metadata, file path, and current spec file content. Includes dependency information (both declared dependencies and components that depend on this one).
