# McpServers.Architecture.Tools.UpdateSpecMetadata

Updates spec file metadata (module_name, description, type, dependencies) and syncs changes to database. Only modifies the metadata sections of the spec file, preserving all function/field documentation. Immediately syncs updated metadata to database component entry.
