# MCPServers.Architecture.Tools.GetComponentView

Generates detailed markdown view of a component and its full dependency tree. Returns formatted markdown with component metadata, description, and hierarchical dependency list showing depth levels. Accepts single component ID/module name or list of them.
