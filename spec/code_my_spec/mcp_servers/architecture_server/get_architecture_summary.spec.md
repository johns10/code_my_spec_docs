# McpServers.Architecture.Tools.GetArchitectureSummary

Returns structured architecture metrics for programmatic use. Includes context count, total component count, dependency count, orphaned context count, maximum namespace depth, and circular dependency detection status. Useful for dashboards and health monitoring.
