# CodeMySpec.McpServers.GoogleAdsServer

Anubis MCP server exposing Google Ads tools to AI agents — 7 read tools (auto) + 6 write tools (approval-gated via Claude Code's built-in tool-permission system). The agent surface for story 817; registers each tool via component(...). Backed by the GoogleAds.Tools wrappers over CodeMySpec.Google.Ads.

## Type

module

## Dependencies

- Anubis.Server
- CodeMySpec.McpServers.GoogleAds.Tools
