# CodeMySpec.ContentAdmin

Editorial ingest layer on the SaaS — fresh-clone-per-sync from `docs_repo`, markdown + YAML frontmatter parsing, parse-status visibility, scheduling, image handling, push trigger to deployed Content clients.

## Type

context

## Dependencies

- CodeMySpec.Content
- CodeMySpec.Git
