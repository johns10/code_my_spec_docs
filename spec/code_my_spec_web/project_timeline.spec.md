# CodeMySpecWeb.ProjectTimeline

The handful of steps a whole project moves through, across the top of its pages, with one marked as where it is now and about two sentences of plain language on that one. A component rather than a page because rule five says it is not a page — it rides the app layout, so where the project stands is there without being navigated to. Deliberately much less than AgentProgressLive (864), which is keyed on sessions and tasks; here all of the story work is a single step.

## Type

liveview_component
