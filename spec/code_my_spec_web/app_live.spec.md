# CodeMySpecWeb.AppLive

The `/app` onboarding wizard + overview. Renders the next missing setup step (account creation, then project creation) until both are done; lands returning users with at least one project on the Overview view.

## Type

live_context

## Dependencies

- CodeMySpec.Projects
- CodeMySpec.Users
- CodeMySpec.Accounts
