# CodeMySpecWeb.MailboxLive

The operator's mail surface in the dashboard: lists the mailboxes the member can see (shared + own; owners/admins see all), opens a mailbox to its per-correspondent threads, and composes/replies (From = the mailbox address). Depends on CodeMySpec.Mail.

## Type

live_context

## Dependencies

- CodeMySpec.Mail
