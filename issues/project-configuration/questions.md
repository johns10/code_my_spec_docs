# Project Configuration — Questions

## Q: Per-component requirement and analyzer overrides

Requirement toggles and analyzer settings will eventually apply down to the
component level (e.g., turn on tests for just this one module). Future story.

## Resolved: Specs and reviews are separate toggles

Split into require_specs and require_reviews. Engineer may want specs
without design reviews or vice versa.
