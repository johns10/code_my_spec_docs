# Mint.WebSocket.Frame



## resolve_fragments(websocket, frames, acc \\ [])

Emits frames for any finalized fragments and stores any unfinalized fragments
in the `:fragment` key in the websocket data structure