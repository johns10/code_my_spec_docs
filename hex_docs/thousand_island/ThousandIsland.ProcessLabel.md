# ThousandIsland.ProcessLabel



## set(name, config)

Sets a process label if the current Elixir version supports it (>= 1.17).

## set(name, config, state)

Sets a process label if the current Elixir version supports it (>= 1.17).