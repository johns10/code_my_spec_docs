# Anubis



## genserver_name(name)

Validates a possible GenServer name using `peri` `:custom` type definition.

## should_compile_cli?()

Checks if anubis should be compiled/used as standalone CLI or OTP library