# Phoenix.CodeReloader.MixListener



## child_spec(init_arg)

Returns a specification to start this module under a supervisor.

See `Supervisor`.

## purge(apps)

Unloads all modules invalidated by external compilations.

Only reloads modules from the given apps.