# Swoosh.Adapters.Sandbox



## all/1

Returns all emails captured for the given owner.

## flush/1

Returns and removes all emails captured for the given owner.

## encode_owner/1

Encodes the given owner pid into a `user-agent` token for browser tests.